server:
  port: 18080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 生产环境数据库配置 - 使用环境变量
    url: jdbc:mysql://host.docker.internal:3306/plant_helper?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: u_plant_helper
    password: $zshpdMiMa1
    # 生产环境连接池配置
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

# 生产环境日志配置
logging:
  level:
    com.tencent.wxcloudrun: INFO
    org.springframework: WARN
    org.mybatis: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: /var/log/plant-helper/application.log
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 30

# 微信小程序配置
wx:
  appid: ${WX_APPID}
  secret: ${WX_SECRET}

# SpringDoc OpenAPI Configuration
springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operations-sorter: method
    tags-sorter: alpha
    display-request-duration: true
  packages-to-scan: com.tencent.wxcloudrun.controller
  paths-to-match: /**
  default-produces-media-type: application/json
  default-consumes-media-type: application/json